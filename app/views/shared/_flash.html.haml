- flash.each do |key, value|
  %center
    .alert.alert-dismissible.fade.show.w-50.mt-3{role: 'alert', class: "alert-#{key}" }
      = value
      %button.close{'aria-label': 'Close', 'data-dismiss': 'alert', type: 'button'}
        %span{'aria-hidden': 'true'} &#215;

